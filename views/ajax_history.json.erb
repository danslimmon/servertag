<%
events = locals[:events]
rslt = {
    "iTotalRecords" => 999,
    "iTotalDisplayRecords" => "20"
}

rslt["aaData"] = events.map do |event|
    diff_list = "<ul>" + event.diffs.reduce("") {|old,d|; old + "<li>#{d}</li>"} + "</ul>"
    [
        event.datetime.strftime("%Y %b %d<br />%H:%M:%S%Z"),
        event.user,
        event.client,
        diff_list
    ]
end
%>
<%= rslt.to_json %>
